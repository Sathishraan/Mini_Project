# Server Configuration
PORT=7007

# MongoDB connection string
# Replace the password with a secure, URL-encoded password if it contains special characters
MONGO_URI=mongodb+srv://sathishraana0701:WyyW2uImlU2MNB55@cluster0.7ajazsu.mongodb.net/Mini_Project
# JWT secret key for signing tokens
# Use a strong, randomly generated key
JWT_SECRET=$(openssl rand -hex 32) # Replace with an actual random key

# Email credentials for Nodemailer using Brevo SMTP
EMAIL_USER=7b53a5001@smtp-brevo.com
EMAIL_PASS=**ReplaceWithSecurePassword** # Regenerate your SMTP password immediately
EMAIL_ID=sathishraana0701@gmail.com

# Frontend URL for password reset links
FRONTEND_URL=http://localhost:5173

# Key for additional security
# Ensure this is also randomly generated if used for cryptographic purposes
KEY=$(openssl rand -hex 16) # Replace with an actual random key

# Twilio credentials for SMS service
# Store Twilio credentials securely; consider environment-specific configurations
TWILIO_ACCOUNT_SID=AC5f5c8d118cfd6346fa9352da07202977
TWILIO_AUTH_TOKEN=**ReplaceWithSecureAuthToken** # Regenerate immediately if exposed
TWILIO_PHONE_NUMBER=+8925315954 # Use the international format with '+' prefix
